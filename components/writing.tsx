import {getPosts} from '@/lib/substack';
import {site} from '@/lib/content';
import {TextLink} from './ui';
export default async function Writing({limit=3}:{limit?:number}){const posts=await getPosts();return posts.length?<div className="post-list">{posts.slice(0,limit).map((p,i)=><article key={p.url} className="post"><span className="post-number">{String(i+1).padStart(2,'0')}</span><div>{p.date&&<time dateTime={p.date}>{new Date(p.date).toLocaleDateString('en-US',{month:'long',day:'numeric',year:'numeric',timeZone:'UTC'})}</time>}<h3><a href={p.url}>{p.title}</a></h3><p>{p.excerpt}</p></div><a className="post-arrow" href={p.url} aria-label={`Read ${p.title} on Substack`}>↗</a></article>)}</div>:<div className="writing-invite"><span className="eyebrow">From my notebook</span><h3>The conversation continues<br/>on Substack.</h3><p>Read my essays and reflections, and subscribe to receive new writing.</p><TextLink href={site.substack}>Visit my Substack</TextLink></div>}
